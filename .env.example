# Copy to .env and fill in. Do not commit .env.

GEMINI_API_KEY=""
APP_URL="http://localhost:3000"

# Email verification — use Gmail to send to any .edu (no domain required)
GMAIL_USER=""
GMAIL_APP_PASSWORD=""

# Optional: Resend (only used if Gmail is not set; free tier may only send to account owner)
# RESEND_API_KEY=""
# FROM_EMAIL="StudyMate <onboarding@resend.dev>"

# Optional
# VITE_API_URL="http://localhost:3001"
# PORT=3001
